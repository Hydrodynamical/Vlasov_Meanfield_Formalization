# Lean build artifacts
/.lake/

# Rendered blueprint output (regenerate with `lake build :blueprint` + `leanblueprint web/pdf`).
# The web/ tree is tracked (checked-in copy / GitHub Pages source); print/ + lean_decls are not.
/blueprint/print/
/blueprint/lean_decls

# plasTeX / LaTeX aux
*.paux
*.pauxl
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.synctex(busy)
