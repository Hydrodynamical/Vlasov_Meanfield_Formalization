## 2026-05-25T20:04:01Z · sketch-author · Vlasov.diagonalCorrection_bound

**Result:** success
**Plan file:** formalize/plans/weakEvolutionEmpiricalMeasure.json
**Validated hints:** 4 kept, 1 dropped
  - `abs_inner_le_norm` → dropped (0 grep matches); substituted with `abs_real_inner_le_norm` (found at Mathlib/Analysis/InnerProductSpace/Basic.lean:467)
  - `Finset.abs_sum_le_sum_abs` → kept as `abs_sum_le_sum_abs` (found in Algebra/Order/BigOperators/Group/Finset.lean)
  - `Finset.sum_le_card_nsmul` → kept (found via @[to_additive] in Algebra/Order/BigOperators/Group/Finset.lean:214)
  - `le_ciSup` → kept (found in Order/ConditionallyCompleteLattice/Indexed.lean:143)
  - `Real.le_sSup` → kept as `le_sSup` (found in Order/CompleteLattice/Defs.lean:83); renamed to drop `Real.` prefix
**Substitutions:** `abs_inner_le_norm` → `abs_real_inner_le_norm` (type-aware: goal contains `@inner ℝ (PhysSpace d)`)
**Patterns matched:** Pattern 2 (real_inner_* over generic inner_* for ℝ-valued inner products) — triggered by `@inner ℝ` in goal; applied by substituting `abs_inner_le_norm` with `abs_real_inner_le_norm`

### Sketch (on success)
```lean
have hN : (0 : ℝ) < (N : ℝ) := Nat.cast_pos.mpr (NeZero.pos N)
have hgradW_nn : 0 ≤ ⨆ x, ‖gradW x‖ :=
  le_trans (norm_nonneg _) (le_ciSup hgradW_bdd (0 : PhysSpace d))
have hgradVφ_nn : 0 ≤ ⨆ z, ‖gradVφ z‖ :=
  le_trans (norm_nonneg _) (le_ciSup hgradVφ_bdd (show PhaseSpace d from default))
have hS : |∑ i : Fin N, @inner ℝ (PhysSpace d) _ (gradW 0) (gradVφ (X t i, V t i))|
    ≤ (N : ℝ) * ((⨆ x, ‖gradW x‖) * (⨆ z, ‖gradVφ z‖)) := by
  calc |∑ i : Fin N, @inner ℝ (PhysSpace d) _ (gradW 0) (gradVφ (X t i, V t i))|
      ≤ ∑ i : Fin N, |@inner ℝ (PhysSpace d) _ (gradW 0) (gradVφ (X t i, V t i))| :=
        abs_sum_le_sum_abs _ _
    _ ≤ ∑ i : Fin N, ‖gradW 0‖ * ‖gradVφ (X t i, V t i)‖ :=
        Finset.sum_le_sum (fun i _ => abs_real_inner_le_norm _ _)
    _ ≤ ∑ i : Fin N, (⨆ x, ‖gradW x‖) * (⨆ z, ‖gradVφ z‖) :=
        Finset.sum_le_sum (fun i _ =>
          mul_le_mul (le_ciSup hgradW_bdd _) (le_ciSup hgradVφ_bdd _)
            (norm_nonneg _) (le_trans (norm_nonneg _) (le_ciSup hgradW_bdd _)))
    _ = (N : ℝ) * ((⨆ x, ‖gradW x‖) * (⨆ z, ‖gradVφ z‖)) := by
        simp [Finset.sum_const, Finset.card_fin, nsmul_eq_mul]
rw [abs_mul, abs_of_pos (by positivity)]
calc (1 / (N : ℝ) ^ 2) * |∑ i : Fin N, @inner ℝ (PhysSpace d) _ (gradW 0) (gradVφ (X t i, V t i))|
    ≤ (1 / (N : ℝ) ^ 2) * ((N : ℝ) * ((⨆ x, ‖gradW x‖) * (⨆ z, ‖gradVφ z‖))) :=
      mul_le_mul_of_nonneg_left hS (by positivity)
  _ = (1 / (N : ℝ)) * (⨆ x, ‖gradW x‖) * (⨆ z, ‖gradVφ z‖) := by ring
```
